DEFINE TABLE interest TYPE ANY SCHEMAFULL PERMISSIONS NONE;

DEFINE FIELD en ON interest TYPE string PERMISSIONS FULL;
DEFINE FIELD de ON interest TYPE string PERMISSIONS FULL;
DEFINE FIELD es ON interest TYPE string PERMISSIONS FULL;
DEFINE FIELD fr ON interest TYPE string PERMISSIONS FULL;
DEFINE FIELD it ON interest TYPE string PERMISSIONS FULL;
DEFINE FIELD zh ON interest TYPE string PERMISSIONS FULL;
DEFINE FIELD ja ON interest TYPE string PERMISSIONS FULL;
DEFINE FIELD hu ON interest TYPE string PERMISSIONS FULL;